# Agente_RAG_FVH
Agente experto en Forraje Verde Hidropónico (FVH) con triaje + RAG + LangGraph.  Flujo: triaje clasifica la pregunta en RESPONDER (usa el RAG sobre el manual FAO), FUERA_TEMA (responde "no lo sé") o ASESOR (deriva a un humano con un mensaje fijo).
